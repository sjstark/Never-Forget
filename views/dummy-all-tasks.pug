<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title = #{title}
    body

        if allTasks
            each task in allTasks
                div 
                    p task.title, task.createdBy.name, task.estimate
                    